# style name or JSON path (default "auto")
style: "auto"
# mouse support (TUI-mode only, -t flag)
mouse: true
# use pager to display markdown
pager: false
# word-wrap at width (0 disables wrapping)
width: 120
# show all files, including hidden and ignored.
all: false
